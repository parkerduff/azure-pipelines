# Central Java/Maven build template — maintained by shared-ci-platform team

parameters:
  - name: jdkVersion
    type: string
    default: '17'
  - name: mavenGoals
    type: string
    default: 'clean package'
  - name: mavenOptions
    type: string
    default: '-B -DskipTests=false'
  - name: runTests
    type: boolean
    default: true
  - name: publishArtifacts
    type: boolean
    default: true
  - name: artifactName
    type: string
    default: 'java-dist'

steps:
  - task: JavaToolInstaller@0
    displayName: 'Install JDK ${{ parameters.jdkVersion }}'
    inputs:
      versionSpec: ${{ parameters.jdkVersion }}
      jdkArchitectureOption: 'x64'
      jdkSourceOption: 'PreInstalled'

  - task: Maven@4
    displayName: 'Maven ${{ parameters.mavenGoals }}'
    inputs:
      mavenPomFile: 'pom.xml'
      goals: ${{ parameters.mavenGoals }}
      options: ${{ parameters.mavenOptions }}
      publishJUnitResults: ${{ parameters.runTests }}
      testResultsFiles: '**/surefire-reports/TEST-*.xml'

  - ${{ if eq(parameters.publishArtifacts, true) }}:
    - script: |
        cp target/*.jar $(Build.ArtifactStagingDirectory)/ 2>/dev/null || true
        cp target/*.war $(Build.ArtifactStagingDirectory)/ 2>/dev/null || true
      displayName: 'Stage build artifacts'

    - task: PublishBuildArtifacts@1
      displayName: 'Upload artifacts'
      inputs:
        pathToPublish: '$(Build.ArtifactStagingDirectory)'
        artifactName: ${{ parameters.artifactName }}

    - script: |
        python $(Build.SourcesDirectory)/build-tools/scripts/publish_artifact.py \
          --name ${{ parameters.artifactName }} \
          --registry Artifactory \
          --build-id $(Build.BuildId)
      displayName: 'Register artifact in Artifactory'
